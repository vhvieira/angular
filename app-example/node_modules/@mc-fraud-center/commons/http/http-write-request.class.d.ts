import { HttpReadRequest } from './http-read-request.class';
export declare class HttpWriteRequest<RQ, RP> extends HttpReadRequest<RP> {
    /**
     * Creates a request object for POST, PUT or PATCH methods.
     * @param url request address
     * @param body content to be sent
     * @param requestType class type to serialize the request body
     * @param responseType class type to deserialize the response body
     * @see https://github.com/weichx/cerialize
     */
    static create<RQ, RP>(url: string, body: any, requestType?: new () => RQ, responseType?: new () => RP): HttpWriteRequest<RQ, RP>;
    body: any;
    requestType?: new () => RQ;
    protected constructor(url: string, body: any, requestType?: new () => RQ, responseType?: new () => RP);
    /**
     * When provided, the `HttpService` will inspect the metadata of this class type to
     * serialize of the request body.
     *
     * @see https://github.com/weichx/cerialize
     */
    withRequestType(requestType: new () => RQ): this;
}

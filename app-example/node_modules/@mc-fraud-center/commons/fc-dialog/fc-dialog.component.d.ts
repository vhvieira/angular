import { EventEmitter } from '@angular/core';
import { ModalDirective, ModalOptions } from 'ngx-bootstrap';
import { Logger } from '@mastercard/ng-commons';
export declare class FcDialogComponent {
    private logger;
    modal: ModalDirective;
    titleDialog: string;
    labelDialog: string;
    labelSecondaryButton: string;
    labelPrimaryButton: string;
    buttonClick: EventEmitter<boolean>;
    enableSecondaryButton: boolean;
    config: ModalOptions;
    constructor(logger: Logger);
    show(): void;
    onSecondaryButton(): void;
    onPrimaryButton(): void;
    checkTitle(): boolean;
}

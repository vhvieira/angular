import { ToastrService as NgxToastrService } from 'ngx-toastr';
import { MessageService, Logger } from '@mastercard/ng-commons';
import { TranslateService } from '@ngx-translate/core';
/**
 * This service provides methods for displaying toast messages
 * (temporary, pop-up messages).
 *
 * There are 4 levels: INFO, SUCCESS, WARNING, ERROR.
 *
 * The service detects if the app is embedded in an iframe. If so,
 * it delivers the toast to the parent (shell) window via `postMessage`.
 * Otherwise, it displays the toast on the current window.
 */
export declare class ToastrService {
    private messageService;
    private logger;
    private translate;
    private ngxToastrService;
    private window;
    private send;
    constructor(messageService: MessageService, logger: Logger, translate: TranslateService, ngxToastrService: NgxToastrService, window: any);
    /**
     * Toasts an info message.
     * @param message the text to display.
     * @param title message title.
     * @param timeToLive in milliseconds.
     */
    info(message: string, title?: string, timeToLive?: number): void;
    /**
     * Toasts a success message.
     * @param message the text to display.
     * @param title message title.
     * @param timeToLive in milliseconds.
     */
    success(message: string, title?: string, timeToLive?: number): void;
    /**
     * Toasts a warning message.
     * @param message the text to display.
     * @param title message title.
     * @param timeToLive in milliseconds.
     */
    warning(message: string, title?: string, timeToLive?: number): void;
    /**
     * Toasts an error message.
     * @param message the text to display.
     * @param title message title.
     * @param timeToLive in milliseconds.
     */
    error(message: string, title?: string, timeToLive?: number): void;
    private postToShell(message, title, timeToLive, severity);
    private postNative(message, title, timeOut, severity);
}

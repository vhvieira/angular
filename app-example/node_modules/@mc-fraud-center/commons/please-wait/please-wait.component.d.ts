import { AfterViewInit } from '@angular/core';
import { ModalOptions, ModalDirective } from 'ngx-bootstrap';
import { Logger, trackByIndex } from '@mastercard/ng-commons';
import { PleaseWaitService } from './please-wait.service';
/**
 * This component displays a static "please wait" modal.
 * Use the `PleaseWaitService` to add/remove messages into it.
 *
 * The modal is automatically shown when a message is added,
 * and closed when there's no more messages left.
 */
export declare class PleaseWaitComponent implements AfterViewInit {
    private service;
    private logger;
    modal: ModalDirective;
    config: ModalOptions;
    messages: string[];
    trackByIndex: typeof trackByIndex;
    constructor(service: PleaseWaitService, logger: Logger);
    ngAfterViewInit(): void;
}

import { ValidatorFn } from '@angular/forms';
/**
 * Validator that forbids controls values to have the following characters:
 * ? ' " < > /
 */
export declare function searchValidator(): ValidatorFn;

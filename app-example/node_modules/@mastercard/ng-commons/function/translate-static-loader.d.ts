import { TranslateLoader, TranslateModuleConfig } from '@ngx-translate/core';
import { Observable } from 'rxjs/Observable';
import { Http } from '@angular/http';
export declare function makeConfig(factoryFn: (...args: any[]) => TranslateLoader): TranslateModuleConfig;
export declare class TranslateStaticLoader implements TranslateLoader {
    readonly http: Http;
    readonly prefix: string;
    readonly suffix: string;
    private apps;
    constructor(http: Http, prefix: string, suffix: string, apps: string[]);
    /**
     * Gets the translations from the server
     * @param lang
     */
    getTranslation(lang: string): Observable<any>;
}

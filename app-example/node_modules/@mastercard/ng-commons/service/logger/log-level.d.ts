/** error > warn > info > debug */
export declare enum LogLevel {
    DEBUG = 0,
    INFO = 1,
    WARN = 2,
    ERROR = 3,
}
/** Factory fn for the LogLevel provider. */
export declare function getDefaultLogLevel(): LogLevel;
/**
 * Changes the default LogLevel for the Logger service.
 * @param level new level.
 */
export declare function setDefaultLogLevel(level: LogLevel): void;

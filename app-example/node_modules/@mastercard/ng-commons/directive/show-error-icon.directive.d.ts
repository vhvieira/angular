import { ElementRef, OnChanges, OnInit, Renderer2 } from '@angular/core';
export declare class ShowErrorIconDirective implements OnChanges, OnInit {
    private el;
    private renderer;
    mcShowErrorIcon: boolean;
    private errorIconElement;
    private divElement;
    private hasError;
    constructor(el: ElementRef, renderer: Renderer2);
    ngOnInit(): void;
    ngOnChanges(): void;
    private addError();
    private removeError();
}
